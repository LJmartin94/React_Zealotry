import { StyleSheet } from 'react-native';

import { ViewTextImgStyle } from '../utils/VTI';

export const SlightTransparency: ViewTextImgStyle = StyleSheet.create({
	view: { opacity: 0.9 },
	text: { opacity: 0.9 },
	image: { opacity: 0.9 },
});
